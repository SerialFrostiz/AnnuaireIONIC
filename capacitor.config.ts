import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AnnuaireContactTest',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
